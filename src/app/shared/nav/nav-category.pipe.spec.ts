import { NavCategoryPipe } from './nav-category.pipe';

describe('NavCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new NavCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
